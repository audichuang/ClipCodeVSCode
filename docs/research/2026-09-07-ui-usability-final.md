# 007ab4a：最後修正複驗與日常 UI 評估

結論：**QA-1～QA-8 這批已知缺陷已完成分輪複驗；這不等於 UI 已經完全清楚、精緻。** 現在是乾淨、可用的工程工具，線圖完成度較高，側欄的資訊組織最值得再整理。

## 最後三項功能複驗

本機同步至 `007ab4a`、重新建置並 Reload Window。使用正式 VS Code 1.136.1、Git 2.55.0、真實 High Contrast Light，寫入操作只在臨時 fixture repo。

- [EXECUTED] QA-6：透過 graph 右鍵選好／壞邊界、點「壞」完成 Bisect。顯示綠色「已找到有問題的提交！」及 c079806 / QA step 4，元凶列也有獨立高亮。兩處都確認，最後點「重設」退出 bisect。
- [EXECUTED] QA-7：Merge 預測 modal 的「Merge 衝突 1 個檔案」已改成深棕色，在白底明顯比上一版可讀。
- [EXECUTED] QA-8：製造 beta 的單檔衝突 → 終端改 README.md 並 git add → 未合併清單為空、MERGE_HEAD 仍在 → 橫幅自動變 1/1，「完成解決」啟用。直接點該按鈕，真正建立雙 parent 的 merge commit，MERGE_HEAD 消失，沒有使用 plugin 的「標記解決」來補救。
- [EXECUTED] 本輪 host 185 passed；graph 2385 passed / 11 skipped、156 files passed；host 與 webview build 成功。E2E／封裝本輪未重跑，上一輪 44ac056 的 7 passing 與封裝結果不能冒充新版結果。

## 視覺與易用性評估

[EXECUTED] 回到 cat 多 repo，使用日常深色主題、約 1100px 寬的視窗，實際查看側欄、歷史線圖、78 檔 PR 比較及 SQL Diff。

依使用者要求，後半段操作走 **icon、分頁、checkbox、OK 按鈕、檔案列與拖曳分隔線**，沒有再用指令面板跳過畫面上的操作流程。repo 篩選、inline／並排、PR 分隔線都實際使用過。最後恢復全部 repo 範圍與原分隔比例；沒有改寫 cat 的檔案、index 或分支。

以下是設計判斷，不是把主觀美感當作功能測試的 pass/fail。

| 區域 | 已做得好的地方 | 仍不夠清楚／精緻的地方 |
|---|---|---|
| 線圖 | HEAD 底色、雙環、分支色條有明確層次；顏色沒有壓過整個畫面。 | 長分支名與 subject 常被截斷；重複的作者／avatar 和完整日期佔掉不少寬度。 |
| PR | 比較兩端、領先數、總增刪與每檔增刪清楚；兩欄可獨立捲動。 | 檔案清單預設偏窄，長檔名與目錄擠在一起。每檔「開啟原生 diff」的藍色按鈕，和頁面主要動作一樣醒目。 |
| Diff | staged／unstaged 分組、inline 預設、紅綠差異與無換行提示容易理解。 | Tab 放整段路徑，與下方標頭重複。增刪統計小且淡；hunk 動作 icon 很小，初次使用不容易看出可點及用途。 |
| 多 repo 側欄 | native TreeView 熟悉、狀態顏色有效；篩選後會顯示 `Filtered: 1/25 repos`，有明確回饋。 | 首開 Staged 107 全展開，第一個 repo 的大量檔案淹沒其他 repo。graph 正在看 common，左邊卻仍是全部 repo；commit 的實際作用範圍不在按鈕附近。 |

具體觀察：同一個 cat workspace，從 25 repo 改成只看 common 後，側欄立刻從滿屏相似的 SKILL.md／路徑變成兩個 SQL 檔，清楚很多。但必須先開漏斗、取消全選、選 repo、確認，才能得到這個焦點視圖。這是第一眼資訊組織的差距，不是資料讀取錯誤。

PR 拖寬檔案欄確實有效，所以不需要另外造一套檔案瀏覽器；應優先改善預設比例與檔名／目錄的寬度分配。在目前寬度下 inline 比並排更好讀，保留 inline 預設合理。

## 建議下一輪只做的三件事

1. **側欄先呈現範圍與摘要**：大量變更時先露出 repo 摘要、避免首個 repo 全展開吃滿。Commit 附近直接顯示會提交幾個 repo／幾個檔案，區分「graph 當前 repo」與「左側操作範圍」。
2. **長檔名優先可讀**：Diff tab 僅顯示檔名，完整路徑留在已有標頭／tooltip；PR 檔名優先保留，目錄降為次要資訊。線圖作者／日期欄再緊湊一點，把空間還給分支與 subject。
3. **減少主次操作競爭**：保留 icon 操作，但次要的「開啟原生 diff」改成中性工具按鈕；hunk 操作給更清楚的 hover／可點提示與足夠熱區；增刪 badge 的字色、間距再調整。

不建議為了「好看」新增動畫、漸層、更多面板或更多功能。先完成以上資訊層次與密度整理，再用同一組真實 cat 資料看前後差異。

## 不能宣稱的範圍

沒有保證所有視窗寬度、主題、資料量、Git 版本及操作組合零缺陷；11 個 skipped 測試仍未覆蓋。已知 rename 的 per-hunk 不支援情況仍會先讓使用者點按鈕，再拒絕並說明，事先禁用的改善尚未做。

因此驗收措辭應為：**這批已知問題已關閉，主要操作可用；還需要一輪有範圍的視覺／資訊設計收斂，不能說「UI 完全沒問題」。**
